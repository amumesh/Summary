import React, { Component } from "react";

class DropDown extends Component {
    render() {
        return <select className = "ddl" >
            <
            option > Select language < /option> <
        option > English < /option> <
        option > Hindi < /option> <
        option > Marathi < /option> < /
        select >
    }
}

export default DropDown